version: 0.2

env:
  parameter-store:
    DOCKER_REGISTRY_USERNAME: /cicd/docker-credentials/username
    DOCKER_REGISTRY_PASSWORD: /cicd/docker-credentials/password
    DOCKER_REGISTRY_URL: /cicd/docker-registry/url
    SONAR_TOKEN: /cicd/sonar/sonar-token

phases:
  install:
    runtime-versions:
      python: 3.11
      java: corretto17
    commands:
      - echo "Installing Trivy..."
      - wget https://github.com/aquasecurity/trivy/releases/download/v0.19.2/trivy_0.19.2_Linux-64bit.tar.gz
      - tar zxvf trivy_0.19.2_Linux-64bit.tar.gz
      - export PATH=$PATH:$PWD

      - echo "Installing OWASP Dependency-Check..."
      - wget https://github.com/jeremylong/DependencyCheck/releases/download/v7.0.2/dependency-check-7.0.2-release.zip
      - unzip dependency-check-7.0.2-release.zip

      - echo "Installing SonarQube Scanner..."
      - curl --create-dirs -sSLo $HOME/.sonar/sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.7.0.2747-linux.zip
      - unzip -o $HOME/.sonar/sonar-scanner.zip -d $HOME/.sonar/
      - export SONAR_SCANNER_HOME=$HOME/.sonar/sonar-scanner-4.7.0.2747-linux
      - export PATH=$SONAR_SCANNER_HOME/bin:$PATH
      - export SONAR_SCANNER_OPTS="-server"

  pre_build:
    commands:
      - echo "Running Trivy filesystem scan..."
      - ./trivy filesystem --exit-code 0 --no-progress . >> trivyfilescan.txt

      - echo "Logging into Docker..."
      - echo "$DOCKER_REGISTRY_PASSWORD" | docker login -u "$DOCKER_REGISTRY_USERNAME" --password-stdin "$DOCKER_REGISTRY_URL"

  build:
    commands:
      - echo "Building Docker image..."
      - export IMAGE_NAME="$DOCKER_REGISTRY_URL/$DOCKER_REGISTRY_USERNAME/swiggy:latest"
      - echo "Image name: $IMAGE_NAME"
      - docker build -t "$IMAGE_NAME" .

      - echo "Pushing Docker image..."
      - docker push "$IMAGE_NAME"

  post_build:
    commands:
      - echo "Running Trivy image scan..."
      - ./trivy image "$IMAGE_NAME" >> trivyimage.txt

      - echo "Running OWASP Dependency-Check..."
      - ./dependency-check/bin/dependency-check.sh --project swiggy --scan . --format ALL

      - echo "Running SonarQube analysis..."
      - sonar-scanner -Dsonar.projectKey=swiggy \
                      -Dsonar.sources=. \
                      -Dsonar.host.url=http://15.206.27.31:9000 \
                      -Dsonar.login=$SONAR_TOKEN \
                      -X

artifacts:
  files:
    - appspec.yaml
    - trivyfilescan.txt
    - trivyimage.txt
